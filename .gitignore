# dai — nada de andamiaje ni artefactos de OS
.DS_Store
*.log
node_modules/
.dai/          # scaffolding que genera `dai init` en repos target (no acá)
.env           # SECRETOS del adaptador de PM. Solo se versiona .env.example
*.tgz          # tarball de `npm pack`
coverage/      # reportes de cobertura
scratch/       # borradores locales
